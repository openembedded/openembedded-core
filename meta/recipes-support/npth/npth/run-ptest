#!/bin/sh

cd "$(dirname "$0")/tests" || exit 1

for t in t-mutex t-thread t-cond t-fork; do
    [ -x "./$t" ] || continue
    if "./$t"; then
        echo "PASS: $t"
    else
        echo "FAIL: $t"
    fi
done
